meta {
  name: Admin Telemetry
  type: http
  seq: 1
}

get {
  url: {{base_url}}/admin/telemetry
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

docs {
  FR-098. Admin role required in production.
}

tests {
  test("status is 200 or 403", function() {
    expect(res.status).to.be.oneOf([200, 403]);
  });
}
